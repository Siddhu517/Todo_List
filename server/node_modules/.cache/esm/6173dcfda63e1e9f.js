let express,cors,dbConnection,Routes,dotenv;_a63‍.w("express",[["default",["express"],function(v){express=v}]]);_a63‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_a63‍.w("./dbConnection/db",[["dbConnection",["dbConnection"],function(v){dbConnection=v}]]);_a63‍.w("./routes/route",[["default",["Routes"],function(v){Routes=v}]]);_a63‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);




dotenv.config();

const app = express();

app.use(express.json({ extended: true }));
app.use(express.urlencoded({ extended: true }));
app.use(cors({ origin: process.env.CLIENT_URL }));

//route
app.use("/", Routes);

//db
const url = process.env.DATABASE_URL;
dbConnection(url);

const port = process.env.PORT || 8001;
app.listen(port, () => console.log(`Server Running on port:${port}`));
